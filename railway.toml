[build]
builder = "NIXPACKS"
buildCommand = "cd client && npm ci && npm run build && cd ../server && npm ci && npx prisma db push"

[deploy]
startCommand = "cd server && npm start"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
